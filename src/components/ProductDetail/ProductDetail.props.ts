export interface IProps {
  id: number;
  images: string[];
  title: string;
  description: string;
  brand: string;
  rating: number;
  stock: number;
  price: number;
  discountPercentage: number;
}